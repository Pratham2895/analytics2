#data frame

(rollno = c(paste('S-',1:30, sep='')))
(sname = c(paste('student',1:30, sep='','-xyz')))
(gender = sample(c('M','F'), size=30, replace=T, prob = c(.6, .4)))
(age = rnorm(30, mean = 25, sd = 7))
(course = )
